# PostCard

A post card on `card` with `radius-lg`: header, photo with `radius-md` inside, caption without a handle, text actions. A cross-gender business post gets `business` and `wallText`. The photo comes from the consumer as `photoStyle` with a background or a node in `photo`.
