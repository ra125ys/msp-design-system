# PostCard

Карточка поста на `card` с `radius-lg`: шапка, фото со скруглением `radius-md` внутри, подпись без ника, действия текстом. Кросс-гендерный бизнес-пост получает `business` и `wallText`. Фото — потребитель даёт `photoStyle` с фоном или узел в `photo`.
